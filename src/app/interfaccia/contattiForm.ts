export interface IRegione {
    regione: string;
    id: string;
}

export interface IProvincia {
    provincia: string;
    id: string;
}

export interface IComune {
    istat: string;
    nome: string;
}