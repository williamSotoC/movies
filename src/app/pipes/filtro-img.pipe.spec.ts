import { FiltroImgPipe } from './filtro-img.pipe';

describe('FiltroImgPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroImgPipe();
    expect(pipe).toBeTruthy();
  });
});
