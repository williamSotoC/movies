import { FiltroImgBackgroundPipe } from './filtro-img-background.pipe';

describe('FiltroImgBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroImgBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
